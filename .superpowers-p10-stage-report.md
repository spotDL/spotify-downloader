# Plan 10 · Stage (Tasks 2 & 3) — TS client + app shell

**Worktree:** `/Users/xnetcat/Projects/xnetcat/spotdl-v5-p10` · **Branch:** `p10-main`
**HEAD:** `c30e22d1` (Task 3) on top of `8d2cb889` (Task 2)
**Gate:** web `lint` + `type-check` + `test` (7 files / 24 tests) + `build` all GREEN; `make web-clients-check` clean and deterministic (regen → empty git diff).

## State absorbed from the killed predecessor

- **Task 2 was already committed** (`8d2cb889`, exact brief message). Verified complete: generated tree under `src/api/generated/`, `ws-types.gen.ts` / `ws-protocol.gen.ts`, `openapi-ts.config.ts`, `scripts/gen-ws-types.mjs`, `generate:api`/`generate:ws` scripts, `Makefile` `web-clients`/`web-clients-check` targets, `generated-banner.test.ts` + `error-codes.test.ts` all present and green. `make web-clients-check` passes (deterministic).
- **Task 3 was partial/uncommitted.** Implementation files were mostly written (client skeleton, stores, config gate, router, queries, errors, components, routes, typed MSW fixtures/handlers). I finished and committed it as `c30e22d1`.

## What I did to finish Task 3

1. **Fixed a type-check blocker in `src/api/errors.ts`.** `ApiError`'s constructor used TS parameter properties, disallowed under `erasableSyntaxOnly`. Rewrote with explicit field declarations. (This was the only thing keeping type-check red.)
2. **Wrote the four required Task 3 tests** (all green):
   - `src/api/base-url.test.ts` — default → same-origin (`resolveHttpBase()===""`, `createClientConfig({}).baseUrl===""`); override normalized (trailing slashes stripped, whitespace trimmed); ws scheme mapping (same-origin `ws://host`, https→`wss`, http→`ws`).
   - `src/api/error-table.test.ts` — every generated `ErrorCode` (read via `?raw`, same technique as `error-codes.test.ts`) has a message + severity; detail interpolation (`rate_limited`, `download_failed`); graceful degradation for an unknown future code.
   - `src/app/config-gate.test.tsx` — success → app shell renders; `/config` error → recovery screen with base-URL field (router NOT rendered); entering a URL writes `useSessionStore.apiBaseUrl` and retry succeeds.
   - `src/routes/app-shell.test.tsx` — gated nav links appear/hide per config mode (selfhost/hosted/embedded).
3. **Added `src/test/render-app.tsx`** — mounts the real shell (QueryClient → Theme → ConfigGate → RouterProvider) with a fresh QueryClient + memory-history router per call.
4. **Test-harness fix in `src/test/setup.ts`:** point the generated hey-api client at `location.origin`. In jsdom the runtime `fetch` is node/undici, which rejects the relative URL that a same-origin base (`""`) produces ("Failed to parse URL"). This makes requests absolute so MSW's `*/api/v1/...` patterns match. It does **not** touch `resolveHttpBase` (CONTRACT B is unit-tested directly).
5. **Excluded colocated `*.test.tsx` from the route tree:** added `tsr.config.json` (`routeFileIgnorePattern`) and the same option to `TanStackRouterVite` in `vite.config.ts`, so `routes/app-shell.test.tsx` no longer trips a `tsr generate` warning nor pollutes `routeTree.gen.ts`.

## Concerns to hand forward

- **`make check` (full, incl. Python) not run.** Per the stage gate I ran the four web commands + `web-clients-check` (all green). Changes are web-only; Python is untouched. If a fan lane needs the full `make check`, it is expected green.
- **`@hey-api/client-fetch@0.13.1` deprecation** (carried from Task 1's report) is unresolved but non-blocking; the v0.99 bundled client under `generated/client/` is what's actually used. `client.ts` imports `CreateClientConfig` type from `./generated/client`.
- **Per-request base-URL re-read is Task 4.** The client's `baseUrl` is captured at init from `resolveHttpBase()`; the config-gate recovery flow writes the store but a live switch needs the Task 4 interceptor (and a reload). The base-URL test asserts the resolver, not a live client swap.
- **Import boundary:** `setup.ts` and `render-app.tsx` import generated code directly. These are test-harness files; Task 5's `api-boundary.test.ts` should treat test files as exempt.
- **Bundle size warning** (>500 kB single chunk) on build — cosmetic, code-splitting deferred.

## Fan worktrees (created serially from `c30e22d1`)

`spotdl-v5-f10a` `f10b` `f10c` `f10d` — all at `c30e22d1`.
