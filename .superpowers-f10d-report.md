# Plan 10 Task 10 — Settings page + admin-lite (track f10d)

## Status: complete. All four gates green.

## What landed
- **`routes/settings.tsx`** — full page replacing the placeholder:
  - Read-only server-config panel (mode, matcher_version, per-flag Features, and
    `download_defaults` only when the server provides them). No mutation exists.
  - Appearance: theme segmented control → `useUiStore.setTheme`.
  - Connection: API base URL field → `useSessionStore.apiBaseUrl`; validates
    http(s)/blank; **Save** persists + `queryClient.clear()`; **Test connection**
    probes `checkHealth()` → `{resolveHttpBase()}/api/v1/health`.
  - Account (gated `Feature flag="auth"`): sign-out clears in-memory access token
    + persisted refresh token + query cache, then navigates home.
- **`routes/admin.tsx`** — tab layout, `beforeLoad` guard `auth && is_admin`
  (warms the shared `["me"]` query via `ensureQueryData`; non-admin → `/`,
  signed-out/rejected token → `/login`).
- **`routes/admin.index.tsx`** — `AdminStatsResponse` stat cards.
- **`routes/admin.reports.tsx`** — status-filtered queue + approve/reject with an
  optional note; both invalidate `["admin","reports"]` + `["admin","stats"]`.
- **`routes/admin.users.tsx`** — paged (`PagedUsers`) table with prev/next.
- **Tests:** `settings.test.tsx` (10 cases inc. read-only config per mode,
  base-URL persist+cache-clear, no-config-mutation-by-construction) and
  `admin.test.tsx` (non-admin redirect, admin stats, approve → invalidations).

## Gate summary
- `pnpm -C apps/web run lint` — 0 errors, 24 warnings (all pre-existing
  `only-export-components`, same class the scaffold routes already emit).
- `type-check` — clean.
- `test` — 9 files, 35 tests passing (was 24).
- `build` — succeeds (informational >500 kB chunk warning, pre-existing).

## Shared-file edits (additive only)
- `api/queries.ts` — added the app-hook boundary for this task: `useMe` /
  `meQueryOptions`, `checkHealth`, `useAdminStats/Reports/Users`,
  `useApproveReport/useRejectReport` (stable `["admin",…]` keys so review
  invalidations hit every status + the stats cards).
- `routes/__root.tsx` — added an `Admin` nav link gated on `is_admin` (hidden
  by default; existing app-shell tests unaffected — no token → link absent).
- `test/msw/fixtures.ts` — added `makeDownloadDefaults`, `makeAdminUser`,
  `makePagedUsers`, `makeReport`, `makePagedReports` (typed against the
  generated schema).

## MERGE notes (design-system sibling track)
Local placeholders marked `// MERGE:` for: `<SettingsSection>` / `<DefinitionRow>`,
`<SegmentedControl>` (theme + report-status filters), admin `<Tabs>`,
`<StatCard>` grid, and users `<DataTable>`.

## Concerns
- The admin guard depends on `GET /auth/me` succeeding, which needs Task 4's auth
  interceptor to attach the access token in production. The guard is written to
  degrade gracefully (me-query failure → redirect to `/login`).
- Sign-out is local-only (clears client state); server-side refresh-token
  revocation is CONTRACT C / Task 4, intentionally not wired here.
