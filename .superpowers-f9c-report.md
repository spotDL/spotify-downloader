# Plan 9 Track C (f9c) — settings + auth/admin screens

Worktree: `/Users/xnetcat/Projects/xnetcat/spotdl-v5-f9c` (branch `f9c`, base `4f10822d`).

## Task 9 — data-driven settings screen — DONE
- `tui/widgets/settings_form.py` — dumb, data-driven form: one control per
  `SettingField` (Switch/Select/Input), emits `SettingsForm.FieldChanged`.
- `tui/screens/settings.py` — delegates validate/save to `SettingsViewModel`; inline
  validation line; `ctrl+s` save; `api_url` change raises a reconnect notice.
- Registered `settings` in `SECTION_SCREENS` (tui/app.py); added `.settings-*` rules
  to `app.tcss`.
- `tests/tui/test_screen_settings.py` — 4 pilot tests.
- Gate: settings+budgets+guards+shell 22 passed; full tui 140 passed; ruff clean;
  mypy apps/cli/src clean.

## Task 10 — auth + admin screens — DONE
- `tui/screens/auth.py` — identity line + login/register/logout form over
  `AuthViewModel`; posts `AuthChanged` on success (app re-runs `refresh_auth`,
  repaints StatusBar); `can_auth` False renders the copy-locked `OFFLINE_AUTH_MESSAGE`
  with no form.
- `tui/screens/admin.py` — reports queue (DataTable) + stats line; `a`/`x`
  approve/reject the focused row via `AdminViewModel` and update the status cell
  (note passed as None — no modal prompt in v1).
- Registered `auth`/`admin` in `SECTION_SCREENS`; added auth/admin styles to `app.tcss`.
- `tests/tui/test_screen_auth.py` (5) + `tests/tui/test_screen_admin.py` (4).
- Gate: full tui 149 passed; full apps/cli 403 passed; ruff + format clean (repo);
  mypy apps/cli/src clean; budgets/guards pass.

## Shared-file edits
- `tui/app.py` — imports + `SECTION_SCREENS` for settings/auth/admin.
- `tui/app.tcss` — settings/auth/admin styles.
- `tests/tui/test_app_shell.py` — `_admin_setup()` now sets `stats_result`
  (the admin section mounts the real `AdminScreen`, which loads stats, replacing the
  placeholder the Task 4 test relied on).
