// The Control Room icon set is lucide-react. This module re-exports the lucide
// glyphs under the app's legacy `*Icon` names so existing call sites keep
// working; icons accept `className` (e.g. `size-5`) and inherit `currentColor`.
export {
  Search as SearchIcon,
  Download as DownloadIcon,
  Library as LibraryIcon,
  Settings as SettingsIcon,
  RefreshCw as RefreshIcon,
  Flag as FlagIcon,
  Play as PlayIcon,
  ExternalLink as ExternalIcon,
  ChevronRight as ChevronRightIcon,
  Disc3 as DiscIcon,
  User as UserIcon,
  Music as NoteIcon,
} from "lucide-react";
