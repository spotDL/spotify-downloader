import { createFileRoute } from "@tanstack/react-router";

// Placeholder index route (Task 3 implements the real home page).
export const Route = createFileRoute("/")({
  component: () => <div>spotDL</div>,
});
