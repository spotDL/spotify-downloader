"""Textual presentation layer (screens, widgets, app) — filled in from Plan 9 Task 4.

Kept as an importable package from Task 1 so the ``tui_presentation`` import-linter
contract (CONTRACT I) has a resolvable source module. Screens and widgets added
here may import only ``textual``, ``rich``, and ``spotdl_cli.viewmodels``.
"""
