# Plan 10 · Task 1 — Scaffold fixups, dependency floors, styling & test harness

**Worktree:** `/Users/xnetcat/Projects/xnetcat/spotdl-v5-p10` · **Branch:** `p10-early`
**Commit:** `f3c34891cc6b4ea4c85bdd361ccebf607bb3d072`
**Gate:** `make check` GREEN (Python: 1315 passed / 9 skipped; web: lint + type-check + test + build all green).

## What was done

### (a) Carried-forward ledger gaps
- `apps/web/package.json`: added `"packageManager": "pnpm@11.10.0"`.
- `apps/web/README.md`: notes Node ≥22 and pnpm 11.10.0.
- `tsconfig.node.json`: `include` → `["vite.config.ts", "vitest.config.ts", "openapi-ts.config.ts"]`, `types` → `["node", "vitest"]`. `vitest.config.ts` now type-checks; `openapi-ts.config.ts` is pre-listed for Task 2 (a missing include pattern is silently ignored by tsc, so no error today).
- jest-dom: `src/test/setup.ts` does `import "@testing-library/jest-dom/vitest"`; `vitest.config.ts` sets `test.setupFiles: ["src/test/setup.ts"]` (globals kept). `App.test.tsx` now asserts `toBeInTheDocument()` and type-checks — the side-effect import augments vitest's `Assertion` project-wide, so no extra `types` entry or `vitest.d.ts` was needed.

### (b) Dependency floors (CONTRACT A1)
Runtime: `@tanstack/react-router@^1.170.17`, `@tanstack/react-query@^5.101.2`, `zustand@^5.0.14`, `@hey-api/client-fetch@0.13.1` (exact), `tailwindcss@^4.3.2`, `@tailwindcss/vite@^4.3.2`.
Dev: `@tanstack/router-plugin@^1.168.19`, `@tanstack/router-devtools@^1.167.0`, `@tanstack/react-query-devtools@^5.101.2`, `@hey-api/openapi-ts@0.99.0` (exact), `json-schema-to-typescript@15.0.4` (exact), `msw@^2.15.0`, `@playwright/test@^1.61.1`, `@testing-library/user-event@^14.6.1`, **plus `@tanstack/router-cli@^1.167.18`** (not in the brief's list, but required to provide the `tsr` binary the scripts call). `pnpm-lock.yaml` committed.

### (c) Tailwind v4 + theme + router harness
- `vite.config.ts`: `TanStackRouterVite({ target: "react" })` (before `react()`) + `tailwindcss()`.
- `src/index.css`: rewritten per CONTRACT H (`@import "tailwindcss"`, `@custom-variant dark (...)`, `@theme` brand/bg/surface/fg/muted/accent/danger/warn tokens + font/radius/shadow, dark-token overrides). Build CSS grew 0.27 → 6.34 kB, confirming Tailwind processes.
- `stores/ui.ts`: zustand `persist("spotdl-ui")` with `theme`/`navCollapsed`/`setTheme`/`toggleNav`.
- `src/app/theme.tsx`: `ThemeProvider` stamps `document.documentElement.dataset.theme`, resolving `"system"` via `matchMedia` with a live listener. Wired into `main.tsx`.
- `routeTree.gen.ts` git-ignored; `tsr generate` prepended to `type-check`/`test`/`build` scripts.

### Build joins the gate
- `Makefile` `web-check` → `lint` + `type-check` + `test` + `build` (build last).

### MSW harness
- `src/test/msw/{server.ts,handlers.ts}` + `setup.ts` lifecycle (`listen({onUnhandledRequest:"error"})` / `resetHandlers` / `close`, plus `@testing-library/react` cleanup and a jsdom `matchMedia` stub).

### Tests
- `src/app/theme.test.tsx` (toggling `useUiStore.theme` stamps `data-theme` dark→light) and the fixed `App.test.tsx`. Both green.

## Deferrals (per the task note — no dependency on the generated TS client, which is Task 2)

1. **Anti-`fetch` / import-boundary oxlint rule — NOT ADDED (tooling gap, per brief's fallback).**
   Installed **oxlint 1.73.0 does not implement `no-restricted-syntax`** at all — `oxlint --rules` lists zero "restricted" rules, and a config using it fails to parse (`Rule 'no-restricted-syntax' not found in plugin 'eslint'`). The per-dir override is therefore also unexpressible. As the brief directs, the rule is omitted and the **Task 5 `api-boundary.test.ts` guard test is the authoritative enforcement** for the no-raw-`fetch` / no-stray-`WebSocket` / no-direct-generated-import boundary. The valid `ignorePatterns` (`src/api/generated/**`, `src/api/ws-types.gen.ts`, `src/routeTree.gen.ts`) **were** added and parse cleanly.

2. **Typed MSW fixtures (`fixtures.ts`) + default handlers — DEFERRED to Task 2.**
   The brief requires fixtures typed against `src/api/generated/types.gen.ts` so a schema change breaks them at compile time. That file is produced in Task 2 (which waits on Plan 7's final `openapi.json`). `handlers.ts` ships an empty typed array and `fixtures.ts` is a reserved placeholder with the deferral noted inline; the server/setup lifecycle is fully in place so Task 2 only adds the typed builders + default `selfhost`-config/sample-track handlers.

## Concerns to hand forward

- **`@hey-api/client-fetch@0.13.1` is deprecated** ("Starting with v0.73.0, this package is bundled directly inside `@hey-api/openapi-ts`"; our openapi-ts is 0.99.0). It is installed per the brief/CONTRACT A1 and does not break anything, but **Task 2 should verify** whether to use the bundled client (and how `runtimeConfigPath` maps in the new hey-api layout) rather than the standalone package. This is a CONTRACT A1/A2 detail that may warrant a small amendment.
- **Placeholder routes were introduced.** `tsr generate` exits 1 with no routes directory, so Task 1's "add `tsr generate` to scripts" required a tree to build. Minimal `src/routes/__root.tsx` (root `<Outlet/>`) and `src/routes/index.tsx` were added as clearly-marked placeholders; **Task 3 overwrites both** with the real AppShell/home implementations. `main.tsx` still renders `<App/>` (not the router) — only `ThemeProvider` was wired.
- **pnpm build-script gate.** pnpm 11's pre-script deps check escalates msw's ignored build script to a hard failure. Resolved by committing `apps/web/pnpm-workspace.yaml` with `allowBuilds: { msw: false }` (msw is used only via `msw/node`; its browser worker build is never needed). CI installs must retain this file.
- **Version floors vs. installed.** CONTRACT A1 floors (e.g. router `^1.95`, query `^5.62`) were satisfied with current-latest resolutions (router `1.170`, query `5.101`, tailwind `4.3`), per the contract's "verify latest-compatible at implementation" clause. Generators pinned exact.
